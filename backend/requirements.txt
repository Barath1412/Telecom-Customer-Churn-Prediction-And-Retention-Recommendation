# The API layer only. The pipeline's own dependencies live in ml/requirements.txt
# and must ALSO be installed -- the live narrate endpoint imports src.graph, which
# needs xgboost, scikit-learn, shap and langgraph.
#
#   pip install -r ../ml/requirements.txt
#   pip install -r requirements.txt

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart==0.0.9
httpx>=0.26            # used by fastapi.testclient
pytest>=7.4
