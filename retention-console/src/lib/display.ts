// TODO: display maps — risk band -> {colour role, icon, label},
// lever code -> label, rule_id -> agent-facing text. Section 6, 7.
// Band comes from the API; never compute it from p_churn.
export {}
